// Canonical list of Supporting Document "type" values — shared by
// DocumentManagerSection (add/edit dropdown) and any read-only display of
// documents created through it, so the set of values only lives in one place.
export const DOCUMENT_TYPE_VALUES = ["id_card", "passport", "visa", "certificate", "other"];
